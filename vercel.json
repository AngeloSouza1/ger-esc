{
    "framework": "nextjs",
    "rootDirectory": "app",
    "buildCommand": "npx prisma migrate deploy --schema prisma/schema.prisma && next build --turbopack",
    "installCommand": "npm ci",
    "ignoreCommand": "git diff --quiet HEAD^ HEAD ./app || exit 1"
  }
  